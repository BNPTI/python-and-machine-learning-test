# AI/ML Engineering Challenge — dependências com versões fixadas.
#
# IMPORTANTE: torch/ultralytics só publicam wheels para Python 3.11 e 3.12.
# O `make setup` verifica seu interpretador antes de instalar. Se o `pip install`
# tentar *compilar* o torch a partir do código-fonte, você está na versão errada do Python.
#
# Os wheels CPU-only do torch vêm do índice extra abaixo (sem CUDA, sem GPU necessária).
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Serviço web ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20

# --- Visão / ML ---
numpy==2.1.3
pillow==11.0.0
opencv-python-headless==4.10.0.84
pyyaml==6.0.2
torch==2.5.1
torchvision==0.20.1
ultralytics==8.3.49

# --- Dev / teste ---
pytest==8.3.4
httpx==0.28.1
ruff==0.8.4
