# Copie para .env e ajuste se necessário. Os defaults funcionam sem alteração.

# Caminho para os pesos pré-treinados do YOLO baixados pelo `make setup`.
APP_YOLO_WEIGHTS=yolov8n.pt

# Host/porta para o `make run`.
APP_HOST=127.0.0.1
APP_PORT=8000
